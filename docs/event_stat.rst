ES Class
========

This is the ES (event_stat) class. It is an attempt to go back and rescue
possibly clean baselines over events caught in the INS using the match filter.

.. autoclass:: SSINS.ES
  :members:

  .. automethod:: __init__
